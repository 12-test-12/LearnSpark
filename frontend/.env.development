# 开发环境
VITE_APP_TITLE=LearnSpark 灵犀学习
# 后端接口基础路径（前端 axios 用，所有请求前缀 /api/v1）
VITE_API_BASE_URL=/api/v1
# 后端服务地址（vite proxy 用，必须是完整 URL）
# 本地开发默认指向 localhost:8080；如果用远程服务器，改为远程地址
VITE_BACKEND_URL=http://localhost:8080
