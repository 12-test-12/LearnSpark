<?xml version="1.0" encoding="utf-8"?>
<!--
  LearnSpark · AndroidManifest.xml 补丁
  修复 "packageinfo is null" 错误的关键修复

  修复点：
  1. 显式声明 package="com.learnspark.app"
     - AGP 8.x 默认不要求，但 Capacitor Bridge 启动时仍会读 manifest 的 package
     - 缺失会导致 PackageManager 返回 null PackageInfo
  2. MainActivity 必须有 android:exported="true"
     - Android 12 (API 31) 起强制要求所有带 intent-filter 的组件声明
     - 缺失会导致 PackageInstaller 报 "packageinfo is null"
  3. 添加 <queries> 块（Android 11+ Package Visibility）
     - 允许 WebView 调用 PackageManager 查询其他应用信息
  4. 显式声明 android:networkSecurityConfig
     - 解决 https://localhost 加载时的证书校验问题
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.learnspark.app">

    <!-- ===== 权限 ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ===== Android 11+ Package Visibility ===== -->
    <!-- 不加这个 WebView 在某些设备上 PackageManager 查询会失败 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <!-- 允许查询其他 app（Capacitor Bridge 需要） -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="33">

        <!-- ===== MainActivity（关键修复：exported="true"）===== -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density|fontScale|fontWeightAdjustment|locale|layoutDirection"
            android:name="com.learnspark.app.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== FileProvider（用于分享文件）===== -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
